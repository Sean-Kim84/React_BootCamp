import React from 'react';
import ReactDOM from 'react-dom';

// const template = React.createElement('p', {}, 'test123');
const template = <p>finished the babel presets</p>
ReactDOM.render(template, document.getElementById('app'));
